# SPEC.md — kyoka-grid

<!-- scaffold template v1.8.0 から展開(2026-08-15)。以後このファイルはプロジェクトが育てる -->

## 1. 目的

Q学習エージェントがグリッドワールド(迷路・崖歩き・風の格子)で最適方策を試行錯誤で
獲得していく過程を、価値ヒートマップ・方策矢印・行動軌跡としてリアルタイムに可視化する
教材アプリ。価値反復で厳密に解いた最適方策をテストオラクル兼 UI 機能(一致率表示)として
持ち、「学習が本当に最適解へ収束したか」を検証可能にする。ブラウザ完結・静的エクスポート。

## 2. 機能要求

| ID | 要求 | 優先度 |
|---|---|---|
| F-01 | シード付き PRNG(mulberry32)を注入。core は同一シード+同一操作列で同一結果(決定論) | must |
| F-02 | グリッドワールド環境: 盤面(壁・開始・ゴール・罠・風)、遷移関数 `step(state, action)`。ゴール到達 +1・罠は −100 で開始点へ戻す(崖歩き型)・通常ステップ −0.04。風列は指定方向へ追加移動 | must |
| F-03 | 表形式 Q学習: ε-greedy 行動選択、TD 更新 `Q ← Q + α(r + γ·maxQ' − Q)`。α/γ/ε は実行中に変更可能 | must |
| F-04 | 価値反復ソルバー(オラクル): 最適 V\* と最適行動集合 π\*(タイを集合で保持)を厳密計算 | must |
| F-05 | 可視化: セル背景=状態価値ヒートマップ(正負で色相を分ける)、セル中央=貪欲方策矢印(Q 差で不透明度)、現エピソードの軌跡残像、探索行動時のエージェント色変化 | must |
| F-06 | 実行制御: 1 ステップ実行 / 再生 / 一時停止 / リセット / 速度 1x・10x・100x(100x は描画間引き) | must |
| F-07 | パラメータパネル: α・γ・ε のスライダー。変更は次ステップから即時反映 | must |
| F-08 | 学習曲線: エピソード獲得報酬の移動平均(窓 20)を折れ線表示 | must |
| F-09 | オラクル比較: π\* の半透明オーバーレイ表示トグルと、貪欲方策の一致率 %(到達可能な非終端セルのうち argmax Q ∈ π\* の割合) | must |
| F-10 | 内蔵マップ 3 種以上: 単純迷路 / 崖歩き / 風の格子。マップ切替で学習状態をリセット | must |
| F-11 | 1 ステップモードで直近の Q 更新セルをハイライトし、TD 誤差の符号を色で表示 | should |

## 3. 非機能要求

| ID | 要求 | 検証方法 |
|---|---|---|
| N-01 | 静的エクスポート(`output: "export"`)。サーバ API・外部通信なし | `next build` が out/ を生成 |
| N-02 | `src/core` は純関数のみ・決定論。カバレッジ lines/functions/statements ≥ 90%, branches ≥ 85% | eslint 境界規則 + vitest coverage 閾値 |
| N-03 | 描画(rAF)と学習ステップを分離。100x でも 1 フレームの学習バッチに上限を設け UI をブロックしない | フレーム内ステップ上限のテスト |
| N-04 | ポインタ・タッチ両対応(スライダー・ボタンのみで操作完結) | 手動確認 |

## 4. 品質基準

`npm run verify` green を全ループの完了条件とする(内訳は AGENTS.md §3)。

学習ゲート(シード固定・予算付き。テストにそのままトレースする):

| ID | ゲート | 基準 |
|---|---|---|
| G-01 | 単純迷路の収束 | シード 1、α=0.5 γ=0.95 ε=0.2、500 エピソード(各 ≤200 ステップ)後、到達可能な非終端セルでの方策一致率 = 100% |
| G-02 | 崖歩きの収束 | シード 1、α=0.5 γ=0.95 ε=0.1、1000 エピソード後、一致率 ≥ 90% かつ貪欲方策の評価リターンが最適リターンと一致 |
| G-03 | オラクル自己検証 | 価値反復の V\* が Bellman 残差 < 1e-9(独立再計算) |
| G-04 | 決定論 | 同一シード・同一パラメータで 2 回学習した Q テーブルが深い等値 |

## 5. スコープ外

- 関数近似(DQN・ニューラルネット)。表形式 Q学習のみ
- SARSA 等の他アルゴリズムの切替 UI(将来ループ候補、初期リリースでは持たない)
- マップエディタ・マップ生成器(内蔵マップのみ。生成器は将来ループ候補)
- サーバサイド学習・学習結果の永続化(localStorage 含め初期リリースでは持たない)
- 効果音・BGM
